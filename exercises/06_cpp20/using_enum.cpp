#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

// Using Enum
// using enum Color.

int main() {
    // TODO: Implement the Using Enum feature
    // Hint: using enum Color.

    std::cout << "Exercise using_enum: Implement Using Enum" << std::endl;
    return 0;
}
