#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

// Fold Binary
// (args + ... + init).

int main() {
    // TODO: Implement the Fold Binary feature
    // Hint: (args + ... + init).

    std::cout << "Exercise fold_binary: Implement Fold Binary" << std::endl;
    return 0;
}
